#include<bits/stdc++.h>
#include "LinkedListtemplate.h"
using namespace std;
void reverse(Node* n)
{
    Node * prev = n;
    Node * cur = n->next;
    Node * forw = cur->next;
}
int main(){
    
    return 0;
}